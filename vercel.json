{
  "buildCommand": "cd fertilizer-frontend && npm install && npm run build",
  "outputDirectory": "fertilizer-frontend/dist",
  "framework": null,
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://localhost:8080/api/:path*"
    }
  ]
}
